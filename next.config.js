/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['s.gravatar.com','lh3.googleusercontent.com','platform-lookaside.fbsbx.com'],
  }
}

module.exports = nextConfig
